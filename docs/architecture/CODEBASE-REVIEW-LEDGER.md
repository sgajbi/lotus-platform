# Codebase Review Ledger

This ledger tracks evidence-backed review work in `lotus-platform`.

Companion playbook:

- [CODEBASE-REVIEW-PLAYBOOK.md](./CODEBASE-REVIEW-PLAYBOOK.md)

## Review entries

| Review ID | Date | Scope / Pattern | Status | Findings | Actions Taken | Follow-up | Evidence / Sign-off |
|---|---|---|---|---|---|---|---|
| PLAT-CR-006 | 2026-08-27 | Canonical Docker ownership recovery for deleted checkout labels (#749) | Fixed locally; PR/main/wiki evidence pending | The canonical cleanup plan correctly rejected reused Compose projects but collapsed active foreign owners, deleted checkout labels, and resource-only ambiguity into one blocking shape. After a temporary checkout disappeared, operators had no auditable path between leaving canonical QA blocked and issuing an unsafe manual Docker deletion. Fresh Workbench #875 evidence reproduced orphan Gateway and Advise containers while normal cleanup correctly stayed fail-closed. | Added additive conflict states backed by live path and registered-worktree evidence. Added a separate exact-container retirement command requiring a fresh plan, caller-supplied SHA-256, full identity restatement, canonical-root match, live Docker label reinspection, path absence, Git worktree exclusion, explicit execution confirmation, atomic before/after receipt, and truthful indeterminate-after-mutation posture. The action never removes Compose projects, volumes, images, networks, prefixes, active paths, or ambiguous resource-only owners. Updated operator docs, central/repository context, wiki source, and the front-office runtime skill. | Complete governed Platform feature/PR gates, review, merge, exact-main validation, wiki publication/parity, deployed-skill sync, then use the merged action to reconcile the two proven orphans and rerun Workbench #875 canonical proof. Routing and skill manifest remain unchanged because no task family or skill identity changed. | Focused ownership and retirement tests: `33 passed`; live dry-run receipt: `output/front-office-qa/issue-749-gateway-orphan-dry-run.json`; isolated live fixture `b12a661a4932...` retired while control `fc7546c89aef...` and every pre-existing container remained present, evidence under `output/front-office-qa/issue-749-fixture-20260827183451`; issue evidence: platform #749 and Workbench #875. Final CI, PR, main, wiki, and canonical QA evidence pending. |
| PLAT-CR-005 | 2026-08-24 | Canonical local stack credentials, exposure, observability retention, and runtime safety (#712) | Fixed locally; protected PR/main/wiki evidence pending | The shared Compose baseline embedded database and Grafana credential defaults, enabled anonymous Grafana, published ports on all interfaces, discarded traces, mislabelled Manage telemetry, omitted four scrape jobs, lacked health/resource controls, and overstated which applications it containerised. Runtime proof also exposed an OTel version default that bound receivers to container-localhost despite valid static configuration. | Added idempotent one-time secret bootstrap for PowerShell/POSIX, required secret interpolation, loopback bindings, authenticated Grafana, an optional Caddy local-CA TLS profile, canonical service names, exact telemetry identities, Tempo retention and Grafana datasource, explicit network-reachable OTLP receivers, complete Prometheus inventory, healthchecks and resource ceilings. Application ingress now health-gates application dependencies while validator-enforced observability independence keeps telemetry fail-open. The service scaffolder uses an application anchor so new services cannot reintroduce a telemetry gate. The security validator discovers every supported Compose overlay, rejects alternate list-form service environments, and scans credentials, DSNs, and port bindings so profile syntax cannot bypass controls. Services that reference `host.docker.internal` must carry their own validator-enforced `host-gateway` mapping, preserving native-Linux connectivity without relying on another service's configuration. Added a deterministic fail-closed validator in every repo lane, negative contract tests, bootstrap preservation tests, and truthful README/context/wiki guidance. | Complete Platform feature and PR-merge gates, obtain exact-head review authority, merge with normal history, validate exact main, publish/verify wiki parity, close #712, and remove the branch. No API, schema, migration, financial-data, or production-topology change. | Host-bridge and observability focused suite: `40 passed`; exact-tree PR-merge lane against isolated current remote-main federated sources: `1006 passed, 3 skipped`; base+host-port+TLS Compose `config --quiet` passed with checkout-specific project `lotus-platform-pr721`. Earlier checkout-specific runtime `lotus-platform-issue-712` accepted OTLP HTTP with status 200 and Tempo `/api/traces/1123456789abcdef0123456789abcdef` returned `lotus-platform-issue-712-proof` / `retained-trace-proof`; isolated projects were removed and the shared runtime had no unhealthy running containers afterward. |
| PLAT-CR-004 | 2026-08-10 | Exact background-task cancellation and cleanup reconciliation (#668) | Fixed locally; protected PR/main/wiki evidence pending | Governed background tasks had no identity-qualified cancellation path. Manual termination left deliberately stopped work classified `LOST` with `cleanup_state=PENDING`, even after exact process and Compose cleanup. Review of the first patch also found that repository-target launches used request time as process-start identity, which could reject legitimate ownership or trust a reused PID after slow startup. | Added an exact-task cancellation service and thin PowerShell entrypoint; launch-declared `NONE`, `COMPOSE`, or `UNKNOWN` cleanup ownership; OS-observed process-start identity; PID/start verification; owned-tree-only termination; exact Compose project/working-directory/config-file label verification; fail-closed residual-resource handling; atomic receipt and ledger updates; cancellation-preserving reconciliation; focused Windows/process/Compose isolation tests; and aligned automation docs, task-ledger playbook, repo context, skill source/routing, and the existing Operations wiki. No daemon-wide cleanup, image removal, prefix discovery, or Core runtime change was introduced. | Complete Platform feature/pre-merge validation, signed commits, protected PR/rebase merge, exact-main validation, wiki publication/parity, local skill bootstrap sync, verified #668 closure, and branch cleanup. | Evidence: `tests/unit/test_background_task_cancellation.py` plus `tests/unit/test_agent_engineering_background_runs.py` => `23 passed`; routing/alignment/inventory/runtime pack => `31 passed`; Ruff, Python compilation, PowerShell parsing, engineering-context validation, skill-alignment validation, wiki-quality audit, and diff hygiene passed. |
| PLAT-CR-003 | 2026-07-18 | Culture-stable background-run process identity (#562) | Hardened locally with exact live/terminal proof; PR/main proof pending | The first real repository-native core fan-in launched runner PID 45280 and child make PID 32604, but immediate reconciliation marked it `LOST` while both remained live. PowerShell JSON deserialization produced a typed `DateTime`; passing it to `DateTimeOffset.Parse` triggered culture-dependent string conversion and rejected the July 18 value under the Singapore locale. | Normalized `DateTimeOffset` and `DateTime` values directly to UTC and restricted invariant `RoundtripKind` parsing to string input. Added a real serialized-ledger/live-process regression while preserving wrong-start stale-PID rejection. Exact task `eng-task-20260718-111112-lotus-core-make-profile-derived-state-fan-in` then remained `RUNNING` at immediate reconciliation and reached `SUCCEEDED` with exit code 0, no error, and its required fresh artifact. Updated automation guidance, central/repository context, and the task-ledger playbook. | Merge the bounded #562 fix, validate the exact merge SHA on `main`, then close #562 and delete the feature branch. No wiki change. | Evidence: [PLAT-CR-003-BACKGROUND-RUN-PROCESS-IDENTITY-TIMESTAMP.md](./codebase-reviews/PLAT-CR-003-BACKGROUND-RUN-PROCESS-IDENTITY-TIMESTAMP.md); defect task `eng-task-20260718-105538-lotus-core-make-profile-derived-state-fan-in`; proof task `eng-task-20260718-111112-lotus-core-make-profile-derived-state-fan-in`; proof runner/target PIDs `39596`/`49928`; focused live/stale identity tests `2 passed`; full background-run file `11 passed`; GitHub #562 comments `5009620838`, `5009624987`, and `5009688827`. |
| PLAT-CR-001 | 2026-03-27 | RFC-0068 shared infrastructure ownership and drift detection | Hardened | Initial RFC-0068 slices correctly established ownership, Grafana provisioning, Prometheus ownership, and app-local stack classification, but the first drift guard was lighter on telemetry ownership and the Kafka/topic-bootstrap ownership boundary. `lotus-platform` also lacked a durable review record for the rollout itself. | Tightened the shared-infra ownership validator to cover OTel collector ownership, platform-stack README ownership language, app-local Kafka/telemetry boundary language, and app-local Grafana overlay ownership markers. Wired the validator into `Platform-Pulse`, `Run-Agent`, and the `platform-alignment` and `autonomous-foundation` profiles. Added lower-level tests for validator behavior, automation wiring, platform observability ownership, platform Prometheus ownership, and core app-local overlay ownership. Added this review playbook and ledger so the RFC rollout has durable evidence. | After merge, the next meaningful extension is cross-repo adoption for any other Lotus app that starts carrying app-local observability or shared-infra-like compose assets. No additional RFC-0068 slice is required for `lotus-core` and `lotus-platform` before PR. | Evidence: RFC updates and implementation checklist; `platform-stack/docker-compose.yml`; `platform-stack/prometheus/prometheus.yml`; `platform-stack/grafana/provisioning/dashboards/dashboard.yml`; `platform-stack/otel-collector/config.yaml`; `automation/validate_shared_infra_ownership.py`; `automation/Validate-Shared-Infrastructure-Ownership.ps1`; `automation/Run-Agent.ps1`; `automation/Platform-Pulse.ps1`; `automation/task-profiles.json`; `lotus-core/docker-compose.yml`; `lotus-core/docs/operations/App-Local-Stack-Guide.md`; targeted validation: `pytest tests/unit/test_shared_infra_ownership_validator.py tests/unit/test_shared_infra_ownership_automation_contract.py tests/unit/test_platform_stack_observability_contract.py tests/unit/test_platform_stack_prometheus_contract.py -q` => `9 passed`; `powershell -ExecutionPolicy Bypass -File automation/Validate-Shared-Infrastructure-Ownership.ps1` passed; `pytest tests/unit/test_app_local_observability_contract.py tests/unit/test_app_local_stack_contract.py -q` in `lotus-core` => `6 passed`. |
| PLAT-CR-002 | 2026-07-18 | Governed detached execution for repository-native validation targets (#562) | Hardened | The shared background launcher accepted only catalog profiles, so a long repository-native Make/NPM/script certification target could lose its controller without durable repository, SHA, PID, log, artifact, or terminal-result reconciliation. Review of both background entry points also found that PID-only monitoring could misclassify a reused PID and that direct Windows test launches could leave visible orphan console hosts. | Added a typed, shell-free repository-target mode with exact `repos.json` resolution, Git-root/branch/HEAD/clean-tree fences, safe Make/NPM target validation, in-repository script validation, argv-only job serialization, fresh required-artifact proof, hidden detached Windows startup, bounded Windows initialization retry, runner/target process identity, and atomic ledger writes. Extended the checker to reconcile both modes, reject stale PID reuse by process start time, and persist terminal exit/process-tree evidence. Preserved named-profile behavior. Replaced direct test PowerShell launches with the no-window helper and verified zero orphan console hosts after the exact feature lane. Updated operator docs, central/repository context, skill routing, and the deployed `platform-automation-ops` skill. Same-pattern scan covered `Start-Background-Run.ps1`, `Check-Background-Runs.ps1`, and their new Python execution boundary; no other governed detached launcher or daemon-wide Docker cleanup path was introduced. Design modularity changed; runtime deployment topology did not. | Merge the bounded #562 PR, prove the exact merge SHA on `main`, then reconcile and close #562. No wiki source changed because the durable operator truth is owned by automation docs, context, the review ledger, and the skill; publication is therefore not required. | Evidence: `tests/unit/test_agent_engineering_background_runs.py` => `10 passed`; combined background/agent/context/routing pack => `58 passed`; `Invoke-PlatformRepoChecks.ps1 -Lane feature` => `755 passed, 1 skipped` plus all platform validators green; automation inventory `123 -> 135` scripts and new launcher classified `covered`; collected unit tests `748 -> 756`; source/deployed skill SHA-256 parity true; skill audit `critical=0, high=0, medium=0, low=0`; final process audit `new_orphan_conhosts=0`; GitHub issue #562. |
