{
  "schemaVersion": "lotus-platform.principal-denial-fixture.v1",
  "denialClass": "present_but_unverified",
  "principalKind": "user",
  "environmentPosture": "verified",
  "request": {
    "description": "A well-formed, unexpired, correctly-audienced credential whose signature was never checked against the issuer's keys. Every shape assertion passes: the fields are present, the values are plausible, and the expiry is in the future. Only verification distinguishes it from a genuine principal, which is why a consumer can satisfy every other fixture in this set by validating structure and resolving nothing.",
    "credential": {
      "present": true,
      "issuer": "https://identity.lotus.bank/example-idp",
      "audience": "lotus-gateway:composition",
      "keyId": "key-2026-09",
      "expired": false,
      "revoked": false,
      "subjectKind": "user"
    },
    "tenantRef": "tenant:private-bank-sg",
    "requiredCapability": "manage.write",
    "grantStoreAvailable": true
  },
  "expected": {
    "outcome": "denied",
    "status": 401,
    "failsAtStep": 1,
    "revealsReasonDetail": false,
    "maxOutboundCalls": 0
  }
}
